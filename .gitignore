logs/
authorized_keys
id_ed25519
id_ed25519.pub
nohup.out
.claude/
*.swp
*.swo
*~
bupdaily.conf

# Configbestanden van makevm.sh: die horen bij deze ene host (paden, netwerk)
# en kunnen een wachtwoordhash bevatten - ook de bestanden die
# "makevm.sh --dump-config NAAM" oplevert. makevm.config.voorbeeld eindigt op
# .voorbeeld en valt er niet onder; die blijft wel in git.
makevm*.config
